# PowerShell Prompt Backlog (IT Ops)

1. Collect diagnostics bundle (system info, event logs, network, disk) to a zip
2. Reset Windows Update components (safe mode + verification)
3. Clear Teams cache + relaunch (user-safe)
4. Clear Outlook cache / rebuild OST guidance + checks
5. Printer triage: spooler reset + queue listing + common fixes (non-destructive)
6. Disk cleanup: find large folders + optional safe cleanup actions
7. Network reset: ipconfig/dns winsock + verification steps
8. BitLocker status + escrow verification + recovery key export instructions (no secrets in output)
9. LAPS password retrieval helper (RSAT module checks + guardrails)
10. Sanitize text export using Remove-PiiFromString + file wrapper (already done—convert to prompt card)
